var async = require('../node_modules/async'),
    omdb = require('./');

omdb.search('saw', function (err, movies) {
    async.eachLimit(movies, 5, function (movie, done) {
        omdb.get(movie, console.log);
    });
});
